# Codex 工作指引

**先讀 `_共用規則.md`**，那份是主體，這份只補 Codex 這邊特有的部分。

## 你的定位

這個專案跨兩個 AI 協作：Codex（主要在 Mac app）負責深度討論、企劃、研究、文件產出；Claude Code（主要在 Windows）負責文字整理、SEO／行銷視角、跨專案調度。

你們看的是同一個 repo，靠 `00_現況.md` 交棒。**Claude 寫的東西你當作同事的工作成果看待，不要重做，有疑問寫進現況檔的「待討論」。**

## 開工流程

1. `git status` — 看 Claude 上次動了什麼
2. 讀 `00_現況.md`
3. 動手
4. 更新 `00_現況.md`，交棒
5. `git add -A && git commit -m "做了什麼"`

⚠️ **不要 `git push`**。GitHub repo 尚未建立，push 一定失敗，不用重試也不用除錯。

## 🚨 產出一定要放在這個專案資料夾內

**不要放 `~/Documents/Codex/{日期}/` 那種沙盒目錄。**

那個路徑受 macOS 權限保護，Claude Code 列不到目錄內容，
等於你做的東西它看不見，協作就斷了。

所有檔案寫在專案資料夾內的對應位置：
`討論/`、`研究/`、`產出/`。產完 commit，Claude 下次 `git status` 就知道你做了什麼。

## 討論收尾要存檔

這個專案最有價值的東西是**討論過程**，不是最後那份文件。
一段討論告一段落時，主動把重點寫成 `討論/YYYYMMDD_Codex_主題.md`：

- 問了什麼、結論是什麼
- 哪些方案被否決、為什麼
- 還沒解決的問題

不用逐字，但推理鏈要留下來——Windows 那邊的 Claude 是靠這個接上脈絡的。

## 🎨 改網站前必讀

動 `網站/` 底下任何介面檔案前，先讀 **`網站/DESIGN.md`**。

那份規範是實際修過一輪的結果，不是偏好清單。最常被改壞的三件事：
字型（禁 Arial/Georgia）、對比度（改顏色前先算）、硬陰影（禁用）。

## 產出規則

- 文件產出放 `產出/`，不要放 `~/Documents/Codex/` 那種沙盒臨時目錄（會消失，也不會進 git）
- docx／pptx 產完順手在 `產出/` 放一份同名 `.md` 說明檔，講清楚這份是什麼、何時產的、有什麼已知問題。純二進位檔在 git 裡看不出差異
