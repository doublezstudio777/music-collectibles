# 第 2 階段技術設計：帳號、資料庫、API

2026-09-27，Claude Code on Windows。涵蓋 2a／2b／2c 三段，2a 已施工（程式在 `網站/`），2b、2c 是規劃。
決策出處：`討論/20260923_Claude_企劃四項與命名決策.md`（帳號定案、App 約束、平台定位、測試站不設密碼與資料永久保存、零花費三層防線）。

## 一、硬約束

| 約束 | 來源 | 在設計裡的落點 |
|---|---|---|
| 資料永久保存，測試資料＝正式資料 | 使用者 9/26 | 只用 drizzle 遷移，只加不減；站外備份；軟刪除 |
| 零花費 | 使用者 9/26 | Workers／D1 免費方案（超額是失敗不是扣款）；R2 自己設上限；1 美元預算通知 |
| 之後可能做 App | 使用者 9/26 | 所有讀寫走 `/api/...` 回 JSON；登入同一套 token，網頁放 cookie、App 放 Bearer |
| 開發測試一律本機 | 使用者 9/26 | vinext dev＋Miniflare 模擬 D1／R2；部署前雲端 Workers／D1／R2 維持 0 個 |
| 只做 Email 帳號、開放註冊 | 使用者 9/24 | Email＋密碼＋6 位數驗證碼；Turnstile 擋機器人 |
| 認證帳號＝已驗證 Email | 本輪派工 | `users.email_verified_at` 有值就是認證帳號，檢舉權限看這欄（2b 接） |

## 二、三段分工

| 段 | 內容 | 狀態 |
|---|---|---|
| 2a | 技術設計、D1 遷移、API 層、Email 帳號、Turnstile、點讚／我有／想要／追蹤改存 D1、登入小面板 | 本輪完成 |
| 2b | 內容表進 D1（藝人、系列、品項、版本、炫收藏、照片）、照片上傳 R2、出價、私訊、檢舉、申訴、管理後台權限、正式寄信服務 | 下輪 |
| 2c | 作品頁／藝人簡介維基式編輯與歷史、站外備份排程、零花費三層防線、noindex、部署（部署前先問使用者） | 再下輪 |

## 三、資料表

### 識別碼原則

- 使用者：隨機字串 `users.id`（不外露註冊順序），公開網址用 `handle`
- 內容沿用現在的公開網址當穩定識別碼：炫收藏流水號 `share_no`（`/share/{n}`）、藝人 `slug`、系列 `{slug}/{流水號}`、版本鍵 `{slug}/{流水號}#{品項}-{版本}`。**slug 一經建立不改**；藝人改名只改顯示名稱，要換網址另開轉址表（2c）
- 點讚、我有、想要、追蹤記的是這些公開識別碼，**刻意不對內容表設外鍵**。理由：SQLite 事後加外鍵必須整張重建，違反「禁止重建表」；內容一律軟刪除（維基式要能還原），不會留下孤兒。孤兒檢查放在備份腳本裡順便跑
- 所有時間存 ISO 8601 UTC 字串

### 2a 已建（`drizzle/0000_account.sql`）

| 表 | 主要欄位 | 說明 |
|---|---|---|
| `users` | id、email（唯一，小寫）、email_verified_at、password_hash、handle（唯一）、name、bio、role（user／admin）、status（active／suspended）、created_at、updated_at | 雜湊字串自帶演算法與次數 `pbkdf2-sha256$100000$salt$hash`，之後升級不用改欄位 |
| `sessions` | id＝token 的 SHA-256、user_id、client（web／app）、user_agent、created_at、last_seen_at、expires_at | token 本身不入庫；30 天滑動期限，一天最多更新一次（省寫入額度） |
| `email_codes` | user_id、purpose（verify／reset）、code_hash、attempts、expires_at、used_at | 6 位數，只存雜湊；15 分鐘有效、錯 5 次作廢、60 秒內不能重寄 |
| `rate_limits` | key、count、reset_at | 固定視窗計數：登入 15 分鐘 10 次（每個 Email）、註冊每 IP 每小時 10 次、重寄每 IP 每小時 20 次 |
| `likes` | user_id、share_no、created_at | 點讚＝喜愛清單。主鍵 (user_id, share_no) |
| `holdings` | user_id、kind（owned／wanted）、target_key（版本鍵）、created_at | 我有／想要，公開 |
| `follows` | user_id、artist_slug、created_at | 追蹤藝人，只有本人看得到 |

### 2b 預定（每張都是新表，用新遷移加，不動 2a 的表）

| 表 | 主要欄位 | 說明 |
|---|---|---|
| `artists` | slug（主鍵）、name、aliases（JSON）、kind（藝人／發行單位）、gender、region、tagline、created_by、deleted_at | 簡介正文另存 `revisions` |
| `series` | id、artist_slug、no（同藝人流水號）、title、kind（專輯發行／演唱會…）、year、credits（JSON）、deleted_at | 唯一鍵 (artist_slug, no) |
| `items` | id、series_id、item_id（cd、towel…）、kind（CD／黑膠…）、name、deleted_at | 唯一鍵 (series_id, item_id) |
| `versions` | id、item_id、version_id（v1…）、edition、year、catalog、barcode、marks（JSON）、deleted_at | 版本鍵由上面三層組出 |
| `shares` | no（主鍵，炫收藏流水號）、author_id、what、kind、story、about（JSON）、tags（JSON）、series_key、item_key、version_key、sale_state、price、sold_price、sold_to、sold_at、ref_photo、created_at、deleted_at | 出售狀態直接掛在炫收藏上 |
| `photos` | id、share_no、r2_key、bytes、width、height、created_at、deleted_at | 一則多張；`bytes` 加總就是 R2 用量（見第七節） |
| `offers` | id、share_no、buyer_id、kind（offer／buy）、price、status（open／accepted／rejected／withdrawn／sold）、created_at、updated_at | 公開列表同一買家只顯示最新一筆，舊的留著 |
| `threads`、`messages` | thread：id、share_no、buyer_id；message：id、thread_id、from_id、text、offer_id、system、created_at | 私訊只能從一則收藏發起 |
| `reports` | id、target（`share:{n}`／`item:{鍵}`／`version:{鍵}`）、reporter_id、reason、note、created_at | 唯一鍵 (target, reporter_id)；只有 email_verified_at 有值的帳號能送 |
| `appeals` | id、target、by_id、text、photo_keys（JSON）、status（pending／unlocked／kept）、decided_by、decided_at | 由 admin 裁決 |
| `settings` | key、value | 檢舉門檻（預設 10）、暫停模式開關、R2 用量快取 |

### 2c 預定

| 表 | 主要欄位 | 說明 |
|---|---|---|
| `revisions` | id、target（`artist:{slug}`／`series:{鍵}`）、field（intro／body…）、content、summary（修改說明，必填）、author_id、reverted_from、created_at | 維基式：每次存整份內容，不存差異；「還原到這版」＝新增一筆內容等於舊版的 revision，並署名 |
| `slug_redirects` | old_slug、new_slug | 藝人換網址時用 |
| `usage_daily` | date、r2_class_a、r2_class_b、photo_views | 第七節的讀取計數 |

## 四、API

回應一律 JSON。錯誤統一 `{ "error": { "code": "EMAIL_TAKEN", "message": "這個 Email 已經註冊過，直接登入就好" } }`：`code` 給 App 判斷，`message` 直接顯示。寫入都是「設成某狀態」（`on: true/false`），不是切換，App 斷線重送不會反轉。

### 2a 已做

| 方法 | 路徑 | 登入 | 說明 |
|---|---|---|---|
| POST | `/api/auth/register` | — | email、password、handle、name、turnstileToken → 201，寄驗證碼 |
| POST | `/api/auth/verify-email` | — | email、code（、client） → 驗證並登入 |
| POST | `/api/auth/resend-code` | — | email → 重寄驗證碼（不透露帳號狀態） |
| POST | `/api/auth/login` | — | email、password、turnstileToken（、client: "app"） |
| POST | `/api/auth/logout` | 要 | 刪這個 session、清 cookie |
| POST | `/api/auth/forgot-password` | — | email、turnstileToken → 一律回 ok |
| POST | `/api/auth/reset-password` | — | email、code、password → 重設、**其他裝置全部登出**、這台登入 |
| GET | `/api/me` | 選 | `{ user, state }`，沒登入回 `{ user: null, state: null }`；網頁開站只打這一支 |
| GET | `/api/me/state` | 要 | `{ liked, owned, wanted, follows }` |
| POST | `/api/me/likes` | 要 | `{ share, on }` |
| POST | `/api/me/holdings` | 要 | `{ kind: owned／wanted, key, on }` |
| POST | `/api/me/follows` | 要 | `{ artist, on }`；DELETE＝清掉全部追蹤 |
| GET | `/api/users/{handle}` | — | 公開：名稱、簡介、是否認證、我有／想要。追蹤與點讚不公開 |

`client: "app"` 時 token 放在回應 body，不發 cookie；網頁版 token 只在 HttpOnly cookie，JavaScript 拿不到。

### 2b 預定

`GET /api/shares`（分頁、排序、只看在賣、追蹤中）、`GET /api/shares/{n}`、`POST /api/shares`、`PATCH /api/shares/{n}`（出售狀態）、`POST /api/uploads`（取得上傳位置，見第七節）、`GET /api/artists/{slug}`、`GET /api/series/{slug}/{no}`、`POST /api/shares/{n}/offers`、`POST /api/offers/{id}/respond`、`POST /api/offers/{id}/withdraw`、`POST /api/shares/{n}/close`、`GET /api/threads`、`GET/POST /api/threads/{id}/messages`、`POST /api/reports`、`POST /api/appeals`、`GET/POST /api/admin/...`（role=admin）。

### 2c 預定

`GET /api/revisions?target=`、`POST /api/revisions`（編輯）、`POST /api/revisions/{id}/revert`、`POST /api/internal/budget-alert`（預算通知 webhook，驗簽章）。

## 五、登入流程

```
註冊：填 Email／密碼／帳號名／顯示名稱 ＋ Turnstile
  → POST /api/auth/register → 寄 6 位數碼（本機印在 dev server 終端機）
  → 同一個面板切到「驗證 Email」→ POST /api/auth/verify-email → 登入完成
登入：Email／密碼 ＋ Turnstile → POST /api/auth/login
  ├ 密碼對、已驗證 → 發 session
  ├ 密碼對、未驗證 → 403 EMAIL_UNVERIFIED，自動寄碼，面板切到驗證步驟
  └ 錯 → 401「Email 或密碼不對」（帳號不存在也跑一次雜湊，時間上看不出差別）
忘記密碼：Email ＋ Turnstile → 寄重設碼 → 碼＋新密碼 → 其他 session 全刪 → 登入
```

- 未登入按點讚、我有、想要、追蹤 → 彈登入小面板（不跳頁），登入成功後**補做剛剛那一下並一律設成「打開」**（訪客看到的都是沒按，所以不會把帳號裡已經按過的反而取消），留在原頁
- `/login?next=` 是整頁版，給直接打網址、`/me` 沒登入時用
- 選「6 位數碼」而不是信裡的連結：App 裡不用處理深層連結，手機收信切回來打 6 個數字就好
- 密碼雜湊用 WebCrypto 的 PBKDF2-SHA256，**Workers 上限 100,000 次**（超過直接丟錯，本機 dev 不會擋，只在正式環境爆，[workerd #1346](https://github.com/cloudflare/workerd/issues/1346)）。OWASP 建議 SHA-256 要 600,000 次，100,000 次偏低，靠 Turnstile＋登入頻率限制補。次數跟著雜湊存，平台放寬後登入時重新雜湊即可升級
- **CPU 時間風險**：免費方案每次請求 CPU 上限 10ms（[Workers 限制](https://developers.cloudflare.com/workers/platform/limits/)），100,000 次 PBKDF2 可能超過。只有註冊、登入、重設這三支會碰到。2c 部署後要實測；超過就降次數（例如 50,000）而不是升級付費，這條寫在「部署前檢查」

### CSRF 與 cookie

- cookie：`yz_session`，HttpOnly、SameSite=Lax、https 時加 Secure、30 天
- cookie 驗證的非 GET 請求一律檢查 `Origin` 必須同站，否則 403 `BAD_ORIGIN`；Bearer 不用（瀏覽器不會自動帶）

## 六、權限

| 角色 | 能做 |
|---|---|
| 訪客 | 看所有公開內容、個人頁的我有／想要 |
| 登入（Email 未驗證） | 理論上不存在：未驗證不能登入。重設密碼也會順便標成已驗證（碼寄到信箱＝證明信箱是本人的） |
| 登入＝認證帳號 | 點讚、我有、想要、追蹤；2b 起炫收藏、出價、私訊、**檢舉**、申訴；2c 起編輯作品頁與藝人簡介 |
| admin | 2b：管理後台（檢舉門檻、申訴裁決、停權）。`users.role` 只能用本機腳本或 D1 主控台改，沒有 API 可以把自己升級 |

「認證帳號」目前等於「能登入的帳號」。之後若開放社群登入（Google、LINE），另外加 `verified_level` 欄位區分，不改現有欄位意義。

## 七、R2 容量上限與讀取計數（2b 做上傳、2c 做防線）

R2 免費額度：儲存 10 GB-月、Class A（寫入、列出）每月 100 萬次、Class B（讀取）每月 1,000 萬次、流量免費（[R2 價格](https://developers.cloudflare.com/r2/pricing/)）。

- **容量**：`photos.bytes` 加總＝已用量，每次上傳前在 D1 查一次（不列 R2 物件，列出算 Class A）。總量上限 8 GB（留 2 GB 緩衝），到了就停新上傳、表單顯示一行說明。每人上傳上限（例如每則 6 張、每人 300 MB）同一個查詢算
- **上傳前壓縮**：瀏覽器端縮到長邊 1600px、WebP／JPEG 品質 0.8（現在表單已有 `lib/image.ts` 的 shrink）；伺服器端再擋單檔 > 2 MB
- **讀取**：照片經 Worker 路由 `/img/{key}` 出，加 `Cache-Control: public, max-age=31536000, immutable`，讓 Cloudflare 快取接住大部分讀取，快取命中不算 R2 Class B
- **計數**：Worker 每次真的去 R2 拿（快取沒中）就在 `usage_daily` 加一；月累計超過 800 萬次（Class B 的 80%）改回佔位圖。這個計數本身寫 D1，要注意 D1 每天 10 萬次寫入額度：用「每 50 次寫一次」的批次計數，或只在快取沒中時才寫
- **暫停模式**：`settings.paused = 1` 時停上傳、停照片讀取（回佔位圖），第三道防線（預算通知 webhook）就是把它設成 1

## 八、零花費三層防線（2c）

1. R2 總容量 8 GB 上限停上傳（第七節）
2. 照片快取＋每月讀取計數，接近免費額度改佔位圖（第七節）
3. Cloudflare 預算通知（已建：≥ US$1）改打 webhook 到 `/api/internal/budget-alert`，驗簽章後把 `settings.paused` 設 1

Workers、D1 維持免費方案，超過額度是請求失敗（D1 回錯誤，[D1 價格](https://developers.cloudflare.com/d1/platform/pricing/)），不會扣款。

## 九、遷移策略

- schema 真相源：`網站/db/schema.ts`；改完 `npm run db:generate` 產生 `drizzle/00xx_*.sql`，**產生的 SQL 要人工看過再 commit**
- **只准**：CREATE TABLE、ALTER TABLE ADD COLUMN（要有預設值或可為 NULL）、CREATE INDEX、INSERT 系統設定值
- **禁止**：DROP TABLE、DROP COLUMN、RENAME、改欄位型別或約束。drizzle-kit 碰到這類變更會產生「建新表→搬資料→刪舊表」的 SQL，看到 `__new_` 開頭的暫存表就是它，**一律退回改設計**。欄位不用了就留著、程式不讀；要改語意就加新欄位
- 本機：`npm run db:migrate:local`（`wrangler d1 migrations apply DB --local`，設定檔 `wrangler.local.jsonc`），已套用的記在 `d1_migrations` 表，重跑無變更
- 正式（2c）：部署前先做一次站外備份，再 `wrangler d1 migrations apply --remote`；遷移只在部署流程裡跑，不從任何 API 觸發

## 十、備份策略（2c 實作，設計先定）

- D1 內建 Time Travel：免費方案可還原到 7 天內任一時間點（付費 30 天）（[D1 Time Travel](https://developers.cloudflare.com/d1/reference/time-travel/)）。這是第一層，但它在 Cloudflare 裡面，不算站外
- **站外備份**：每週一次（加每次遷移前），在使用者電腦跑 `wrangler d1 export --remote --output`（SQL 檔）→ 壓縮 → 存到 OneDrive `_個人資料/音藏/備份/D1/`，保留最近 12 份週備份＋每月第一份永久保留
- **R2 照片**：同一支腳本比對 `photos` 表與本機備份資料夾，只下載新增的照片（下載算 Class B，量很小）
- 備份腳本順便跑孤兒檢查（likes／holdings／follows 指到不存在的內容）與筆數守恆（這次匯出的每張表筆數 ≥ 上次，除非有軟刪除）
- 每季做一次還原演練：把最新備份匯入本機 D1，跑首頁與個人頁

## 十一、寄信服務候選（本輪只做介面，寄信印在 console）

介面：`lib/server/services.ts` 的 `Mailer`，由環境變數 `MAIL_MODE` 選；目前只有 `console`。接真的服務只要加一個實作。

| 服務 | 免費額度 | 能不能寄給任何人 | 前提 | 來源 |
|---|---|---|---|---|
| **Resend** | 每月 3,000 封、每天 100 封；超過不扣款，是停寄 | 要先驗證自己的寄件網域，否則只能寄給帳號本人 | 一個能加 DNS 紀錄（SPF／DKIM）的網域 | [Resend 價格](https://resend.com/docs/knowledge-base/what-is-resend-pricing)、[網域驗證](https://resend.com/docs/dashboard/domains/introduction) |
| **Brevo** | 每天 300 封（第三方整理，官方說明頁擋爬蟲沒讀到，**待查證**） | 要驗證寄件者；免費方案信尾可能帶 Brevo 標誌（**待查證**） | 同上 | [Brevo 價格頁](https://www.brevo.com/pricing/)、[第三方整理](https://www.fastlancer.org/en/fastlancer-blog/brevo-review/) |
| Cloudflare Email Service | 免費方案**不能寄給任意收件者**，只能寄給帳號裡驗證過的地址；Workers Paid 才開放（每月 3,000 封內含，之後每千封 US$0.35） | 否 | 要升級 Workers Paid，違反零花費 | [Cloudflare Email Service 價格](https://developers.cloudflare.com/email-service/platform/pricing/) |

**台灣收信可達性**：Gmail、Yahoo 奇摩、Hinet、各校信箱的到達率，三家都**查不到公開數據，待實測**。實測方法：接上之後用同一封驗證信寄到 Gmail、Yahoo 奇摩、Outlook、Hinet 各一個信箱，看進收件匣還是垃圾信。不管哪一家，寄件網域有沒有設好 SPF、DKIM、DMARC 影響最大。

**建議先註冊 Resend**：額度夠朋友測試期用（每天 100 封，一次註冊寄 1 封）、API 最簡單、超額是停寄不扣款、跟零花費原則一致。**卡點是寄件網域**：音藏還沒買網域，要用使用者既有網域的子網域（例如 `mail.dblzm.com` 或 stmusictraveler.com 底下）加 SPF／DKIM 紀錄；只加 DNS 紀錄，不用把整個網域搬到 Cloudflare。寄件人會顯示那個網域，這點要使用者決定。

## 十二、示範資料規則

- 假藝人、假系列、假收藏、假帳號**只准出現在本機 seed**：`網站/scripts/seed-local.mjs`（寫死 `--local`，帶 `--remote` 直接拒絕），**任何 `drizzle/` 遷移都不能 INSERT 示範資料**。驗收時用空資料庫套遷移，確認 users 0 列
- 本機示範帳號：`{帳號}@demo.yinzang.test`／密碼 `yinzang-demo`（小孟 xiaomeng、阿澤 aze…；阿凱 kai 是未驗證 Email 的示範）
- 2a 期間內容仍讀 `lib/data.ts`（示範資料）。2b 把內容搬進 D1 時，示範內容一樣只走 seed；正式資料庫一開始是空的，只放朋友上傳的真實資料
- 部署前（2c）必須做到：正式建置不打包 `lib/data.ts` 的示範內容，頁面改讀 D1

## 十三、本機開發指令

```
cd 網站 && npm ci
npm run db:migrate:local   # 套遷移到本機 D1（.wrangler/state）
npm run db:seed:local      # 本機示範帳號（可重跑）
npm run dev                # http://localhost:5173，寄信內容印在這個終端機
```

Turnstile 本機用官方測試金鑰（站金鑰 `1x00000000000000000000AA`、密鑰 `1x0000000000000000000000000000000AA`，永遠通過；密鑰換 `2x0000000000000000000000000000000AA` 永遠失敗），設定在 `vite.config.ts` 的 `vars`，可用環境變數 `TURNSTILE_SITE_KEY`、`TURNSTILE_SECRET` 蓋掉。測試金鑰說明：[Turnstile testing](https://developers.cloudflare.com/turnstile/troubleshooting/testing/)。

## 十四、部署前檢查（2c，列著不忘）

- 正式 Turnstile 金鑰、寄信 API 金鑰用 `wrangler secret put`，不進 repo
- PBKDF2 在正式環境實測 CPU 時間（第五節）
- noindex：`robots.txt` 全站禁止＋`<meta name="robots" content="noindex">`
- 雲端 D1 建立後第一次遷移前後都做站外備份
- 頁面改讀 D1、正式建置不含示範資料（第十二節）
- `sessions`、`email_codes`、`rate_limits` 過期列的清理（排程或登入時順手刪），避免無限長大
