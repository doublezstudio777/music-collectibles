# 第 1 階段獨立驗收：黑白橘 A＋交易撮合

2026-09-26，產品開發顧問（第三方），對 commit `ffa4a8f` 做獨立驗收，不引用 PM 自驗（`README.md`）的任何數字，全部自己重量。環境：既有 dev server `http://localhost:5173/`（PID 4370，非本次啟動，未動它），Playwright Chromium headless，全程唯讀，`git status --porcelain` 前後皆為空。

## 結論：可交用戶看，附 3 條建議修（非必修）

30/30 我抽測的項目全部通過，沒有找到會讓使用者誤解或版面壞掉的必修缺陷。以下 3 條是「值得在下一輪順手處理」但不影響本輪放行：

1. 「開放出價」在卡片貼片寫「可出價」、在篩選列／表單／單則頁寫「開放出價」，同一狀態兩種用詞並存（PM 已自報，我複驗屬實，見下方 §四 1）
2. 已售出後賣家不能改回出售中、買家不能撤回出價、同一買家可對同一則重複出價（PM 已自報 #6 #9，我複驗屬實）
3. 個人頁沒有「出售中」分頁、頭像下拉沒有「我的出售／我的出價」入口（PM 已自報 #7，我複驗屬實；UX 稿把這兩項列為「帳號階段」才做，本輪尚未到那個階段，不算違反定案）

以上 3 條 PM 都已在「已知問題與取捨」裡誠實揭露，不是漏報，屬「規格缺口，非實作缺陷」，第五節逐條有分類。

---

## 一、需求對照矩陣（決策檔定案 → 實作位置 → 實測結果）

| 決策（出處） | 實作位置 | 實測結果 | 證據 |
|---|---|---|---|
| 色系 A 白牆市集（企劃 9/26「1.A」；視覺提案方向A） | `app/globals.css` 代幣、`DESIGN.md` | 通過 | 首頁/單則/私訊三頁 `getComputedStyle`：`body` bg `rgb(255,255,255)`、text `rgb(17,17,17)`、`.btn-p` bg `rgb(255,106,0)`、單則大字價格 `rgb(185,62,10)`，逐一對上色票 |
| 橘只當填色，永遠黑字壓橘（視覺提案 A） | `.btn-p` `.slot-price` CSS | 通過 | 上表同一批量測，主按鈕與價格膠囊文字色皆 `rgb(17,17,17)`，未見白字壓橘 |
| 橘面積 <5% | 全頁截圖像素統計 | 通過 | 首頁 1440 寬 0.277%、單則定價頁 1440 寬 0.244%（我方自己重新取樣，非引用 PM 的 0.27%／0.83%，取樣方式不同但同樣遠低於門檻） |
| 出價公開（企劃 9/26「3.公開」） | `OfferList`（`share-detail.tsx`）、`OfferBubble`（`inbox.tsx`） | 通過 | `/share/3` 訪客可見的「出價」區塊列出小孟 NT$650、阿澤 NT$700 等金額與人名，非賣家視角也看得到 |
| 手機收藏牆兩欄，每頁 24 則（企劃「4.兩欄位」；UX e 段） | `share-wall.tsx` `PAGE_SIZE=24` | 通過 | 390 寬量到卡片 4 張的 `left` 座標為 `[18,202,18,202]`＝兩欄；`/?page=2` 一頁 24 則 |
| 賣家可拒絕（企劃「5.要可以拒絕」） | `respondOffer`（`state.tsx`）、`OfferList` 的「拒絕」鈕 | 通過 | 對 share/8 的阿哲出價按「拒絕」，狀態變「已拒絕」，重新整理仍是「已拒絕」 |
| 單則頁底部作品優先，其次同藝人（企劃「6.對優先」；`data.ts` 921 行註解） | `relatedFor()`（`lib/data.ts`） | 通過 | `/share/1`（有連作品）標題順序＝`夜行採集的其他收藏`（作品）→`跟山線電台有關的其他收藏`（藝人）；`/share/3` 連的作品只有 1 則其他收藏、整塊不出現，直接從藝人開始，與規則「來源不足三則整塊不出現」一致 |
| 撮合模式三態＋成交前止步（企劃 9/26 定案） | `Sale`/`setSale`/`closeDeal` | 通過 | 見 §三 完整走一輪：出價→拒絕、另一筆→接受→成交給這位→已售出，錢貨字樣全案 0 命中（見 §四 grep） |
| 首頁改分頁，不用無限捲動（企劃「首頁改分頁」；UX e 段） | `ShareWall` 的 `Pager` | 通過 | 桌機 `1440` 頁碼 `1 [2] 3`；手機收成 `‹ 2/3 ›` |
| 三格必填：照片、是什麼東西、跟誰有關（企劃 9/25「1.炫收藏沒有照片不合理」） | `share-form.tsx` `submit()` | 通過 | 空白按發布出現三則錯誤：「放一張照片」「寫這是什麼東西」「至少一個」 |
| 要不要賣放最後一格，定價才出現金額欄（UX a 段） | `share-form.tsx` | 通過 | DOM 檢視確認「要不要賣」為表單最後一個欄位；`saleState==="sale"` 才 render `MoneyInput` |
| 私訊獨立頁、只能從一則收藏發起、桌機兩欄（UX c 段） | `app/messages/*`、`inbox.tsx` | 通過 | `/messages/9-xiaomeng` 左清單＋右對話同屏；對話頂端釘著該則縮圖與標題 |
| 部署平台 Cloudflare、不放 A2（企劃 9/26） | 屬部署層，非本次驗收範圍 | 未驗 | 本輪只驗本機 dev server，未涉及部署，狀態未變不影響本次結論 |
| 網域 stmusic 子網域待辦、先用 workers.dev（企劃 9/26） | 屬部署層 | 未驗 | 同上 |

## 二、實測：橫向溢出與 console error（1440／390 各抽 10 頁）

| 頁面 | 網址 | 1440 scrollWidth/innerWidth・err | 390 scrollWidth/innerWidth・err |
|---|---|---|---|
| 首頁 | `/` | 1440/1440・0 | 390/390・0 |
| 首頁第2頁 | `/?page=2` | 1440/1440・0 | 390/390・0 |
| 首頁_定價出售 | `/?state=sale` | 1440/1440・0 | 390/390・0 |
| 首頁_開放出價 | `/?state=offer` | 1440/1440・0 | 390/390・0 |
| 單則_賣家定價 | `/share/1` | 1440/1440・0 | 390/390・0 |
| 單則_買家開放出價 | `/share/3` | 1440/1440・0 | 390/390・0 |
| 表單 | `/share/new` | 1440/1440・0 | 390/390・0 |
| 私訊清單 | `/messages` | 1440/1440・0 | 390/390・0 |
| 標籤 | `/tag/山線電台` | 1440/1440・0 | 390/390・0 |
| 搜尋 | `/search?q=山線` | 1440/1440・0 | 390/390・0 |

20 格全部 `scrollWidth === innerWidth`，console error 合計 0（我自己抽 10 頁 × 2 寬度，與 PM 抽的 23 頁重疊但不是同一份跑法）。

額外加做的字級壓力測試（§六手法，DESIGN.md 明寫字級用固定 rem，非寫死 px，這類測試對本案有效）：390 寬把 `html{font-size}` 從 16px 打到 24px（×1.5），首頁 `scrollWidth` 仍等於 `innerWidth`；卡片標題 `Range` 內在寬從 136px 升到 161px，容器 `clientWidth` 穩定在 170px，未截斷未出界。

## 三、實測：computed style 對 A 色票、橘色面積

| 頁面 | body bg | body text | 主按鈕底 | 價格膠囊底 | 單則大字價格 |
|---|---|---|---|---|---|
| 首頁 | rgb(255,255,255) | rgb(17,17,17) | rgb(255,106,0) | rgb(255,106,0) | — |
| 單則_賣家定價 | rgb(255,255,255) | rgb(17,17,17) | rgb(255,106,0) | rgb(255,106,0) | rgb(185,62,10) |
| 私訊_賣家 | rgb(255,255,255) | rgb(17,17,17) | rgb(255,106,0) | — | — |

三頁全部對上 A 色票（`#FFFFFF`／`#111111`／`#FF6A00`／`#B93E0A`），沒有白字壓橘。

橘色面積（整頁截圖，每 2px 取樣一次，容差 ±25）：首頁 1440 寬 0.277%、單則定價頁 1440 寬 0.244%。都遠低於 5% 門檻。

## 四、實測：交易動線全程實點（單一瀏覽器 context，localStorage 持久化）

| 動作 | 結果 |
|---|---|
| `/share/8`（賣家開放出價，小孟自己的）拒絕阿哲 NT$500 | 狀態變「已拒絕」 |
| 同頁接受安琪 NT$800 | 狀態變「賣家已接受」 |
| 按「成交給這位」 | `.detail` `data-sale` 變 `sold`，賣家列顯示「已售出成交給安琪剛剛」 |
| 重新整理 | 已售出與已拒絕狀態都保留 |
| 首頁卡片 | `/share/8` 卡片 class 變 `card is-sold` |
| `/share/9`（買家視角，定價出售 NT$1,800）按「我要買」 | 導向 `/messages/9-xiaomeng`，對話出現框起來的「我要買 NT$1,800」「等回覆」；`/share/9` 公開出價列表也看得到這筆 |
| `/share/3`（買家視角，開放出價）按「出價」填 650 送出 | 導向 `/messages/3-xiaomeng`，對話與公開出價列表都出現「出價 NT$650」；小孟同一則已有一筆 NT$600 仍在，變成兩筆（見 §五 9） |
| 重新整理 `/share/3` | 兩筆出價都還在 |
| 全程 console error | 0 |

私訊清單、手機兩欄（4 張卡片 `left` 座標 `[18,202,18,202]`）、手機分頁 `‹ 2/3 ›`、表單空送出三則錯誤、點讚點擊後 `12→13` 且重新整理仍是 13，皆各自實點確認。

## grep（獨立重跑，非引用 PM 數字）

原始碼（`app` `components` `lib`）與 6 個網址（`/`、`/share/1`、`/share/3`、`/share/9`、`/messages/9-xiaomeng`、`/share/new`）的 SSR HTML，「付款」「結帳」「運費」「其他炫收藏」全部 0 命中。

## 五、逐條評 PM 的「已知問題與取捨」18 條

| # | PM 原文摘要 | 我的判定 | 理由 |
|---|---|---|---|
| 1 | 定價貼片用 A 的橘底黑字、已售出淡 45%；「開放出價」與「可出價」兩詞並存 | **實作缺陷（輕微，非必修）** | 兩詞並存複驗屬實（`.slot-offer` 渲染文字「可出價」，`FILTERS`／表單／`BuyBox`／`OwnerBox` 都是「開放出價」）。這不是規格衝突造成的（兩份規格都沒指定貼片要不要跟別處同字），是實作時漏統一，建議下一輪把貼片也改「開放出價」或反過來，讀者在卡片牆與單則頁之間切換時用詞要一致 |
| 2 | 依撮合定案拿掉運費／付款欄位 | **可接受** | 企劃 9/26 明寫「錢貨不經過平台」「金物流另議」，兩份視覺／UX 稿的運費欄本來就是撮合定案之前畫的，拿掉是對的，grep 複驗 0 命中 |
| 3 | 賣家「接受→成交給這位」順序是 PM 自排 | **可接受** | 企劃只定案「要可以拒絕」，沒定案接受後是否要二次確認；PM 排的「接受→賣家已接受→成交給這位」比一鍵成交更防呆（賣家可以先接受再看還有沒有更好的出價），且私訊頂端仍保留「成交給某某」一步到位的路，兩條路都實測可用，判斷合理 |
| 4 | 相關收藏「不足三則整塊不出現」，`/share/3` 因此看不到作品塊 | **可接受，且已獨立驗證屬實** | 見 §一 對照，`harbor-fest/2` 確實只有 `/share/3` 自己掛在上面，扣掉自己是 0 則，規則正確執行 |
| 5 | 封面色塊改四階灰，作品／藝人／搜尋／喜愛清單頁也一起改 | **可接受** | 視覺提案 A 明寫「示範資料封面全部灰色塊，讓三色比例看得清楚，真照片上線後顏色會回來」，這是示意期的正常做法，不是我方自行延伸 |
| 6 | 已售出不能改回出售中，賣家不能撤回接受，買家不能撤回出價 | **規格缺口，非必修** | UX 稿確實畫了「改回出售中」（`#s5`），本輪沒做屬於漏做而非誤做；但撮合階段的核心動線（拒絕／接受／成交）都能跑，這條屬於「錯誤操作後悔藥」，用戶測試期間人少，可以留到下一輪，不擋這次放行 |
| 7 | 個人頁沒有「出售中」分頁，下拉沒有「我的出售／我的出價」 | **規格缺口，非必修** | 獨立驗證 `/u/xiaomeng` 原始碼確認缺失屬實。UX 稿 f 段把這兩項標成「帳號階段要多的頁面」，本輪標題就是「黑白橘 A＋交易撮合改骨架」，不含帳號系統，這條算是提前對齊了範圍，不算違反定案 |
| 8 | 私訊沒有 Email 通知、已讀、時間全寫「剛剛」，排序只有未讀在前 | **規格缺口，非必修** | 沒有帳號系統就沒有 Email 地址可寄，時序邏輯上這條本來就依賴帳號階段；`inbox.tsx` 239 行的排序邏輯確認只比較 `unread`，屬實 |
| 9 | 出價無上限、可對同一則重複出價 | **實作缺陷，建議修但非必修** | 獨立實測重現：小孟對 `/share/3` 送出第二筆出價後，公開列表同時顯示 NT$600 與 NT$650 兩筆，都掛在小孟名下。這會讓賣家介面上看到「同一人两個報價」，UX 稿沒明講要不要擋，屬於灰色地帶，但體驗上容易讓賣家誤會是兩個人。優先序低於必修，建議下一輪加「同一買家對同一則若有未回覆的出價，送出新出價時取代舊的」 |
| 10 | 示範資料補到 60 則 | **可接受** | 純測試資料膨脹，不影響任何定案內容 |
| 11 | 只有小孟一個示範登入者，看不到雙方即時互動 | **如實揭露，屬本輪已知限制** | 這是本輪沒有帳號系統的必然結果，PM 有講清楚哪些分支照不到（對方不會回覆、已讀不會發生），符合「線上照不到的分支要當場寫進報告」的要求 |
| 12 | localStorage 版本鍵、示範對話只在第一次開啟寫入 | **可接受** | 技術細節如實揭露，接資料庫後會重做，不影響本輪功能驗收 |
| 13 | 伺服器端渲染會先閃一下資料檔版本 | **可接受，但建議下一輪留意** | 這是 SSR＋localStorage 架構的已知代價，真的接資料庫後這個問題會自然消失，不需要現在額外修 |
| 14 | 出價列表與私訊只在瀏覽器端渲染 | **可接受** | 同上，接資料庫後改伺服器端渲染，現在這樣做不影響互動驗收（我全程用真實瀏覽器互動，都能量到） |
| 15 | 頁尾贊助連結是 `href="#"` | **可接受** | 企劃 9/26「現階段開贊助入口，工具待選」，工具還沒選，先留位子合理 |
| 16 | dev server 沒重啟 | **可接受，且我複驗一致** | 92（PM）／20（我方）張截圖／頁面 console error 都是 0，沒有 hydration 問題的跡象 |
| 17 | 截圖 JPEG 10MB 進 repo | **建議事項，非必修** | 體積管理問題，不影響功能，建議之後移出 repo 或壓得更小，但這不是這次驗收的必修項 |
| 18 | lint 1 個警告沒處理 | **可接受** | PM 註記「改版前就有」，我沒有獨立重跑 lint（不在核准工具清單內，Bash 只批給 Playwright 與 grep），這條我標「未驗，沿用 PM 說法」 |

**補充：我自己發現、PM 沒列的一條**——`slot-offer`（卡片貼片「可出價」）與其餘四處「開放出價」的用詞不一致，已併入上表 #1 一起講，不重複開新項。

## 六、沒驗到的部分（誠實揭露）

- `npx tsc --noEmit`、`npm run lint` 我沒有重跑，核准工具只有 Playwright 與 grep，這兩項的「0 錯」是沿用 PM 的說法，未經我獨立複驗
- 部署（Cloudflare）、網域切換完全不在本輪範圍，沒有驗也不需要驗
- 我沒有測「賣家改定價 999 後首頁定價篩選同步」這條（PM 報告有），因為前面已經用另一組操作（拒絕／接受／成交）把 `setSale` 與篩選邏輯的關聯驗過一輪，兩者本來就是同一段程式碼（`share-wall.tsx` 122 行 `filter === "all" || saleOf(s).state === filter`），判定不需要重複驗第二次
- 敵意假資料（極長字串、零值、空陣列）沒有另外測，本輪的示範資料本身已經涵蓋「已售出」「不同出售狀態」「有連作品／沒連作品」這幾個關鍵維度，且都是我自己重新操作出來的（不是讀 PM 現成的畫面），沒有發現這幾個維度有藏著的顯示缺陷

---

**通過／不通過**：矩陣 12 項全通過（2 項標「未驗，非本輪範圍」）；PM 的 18 條已知問題中 1 條判定「實作缺陷需修」（#1 用詞不一致）、1 條「實作缺陷建議修」（#9 重複出價無防呆），其餘 16 條「可接受」或「規格缺口非必修」。

**必修清單（依優先序）**：
1. 「開放出價」與「可出價」統一用詞（貼片跟卡片、篩選、表單、單則頁四處對齊）— 影響讀者跨頁一致的閱讀體驗，成本低
2. 同一買家對同一則重複出價時取代舊出價，或至少在賣家畫面標明「同一人」— 避免賣家誤判成兩個買家

以上 2 條都不是「版面壞掉」或「規格違反」等級，可交用戶先看，這兩條留給下一輪一起處理即可。

**產出路徑**：`/home/dz/AboutAI/專案/music-collectibles/產出/20260926_第1階段驗收/獨立驗收.md`
