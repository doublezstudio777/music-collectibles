# 第 1.5 階段獨立驗收（產品顧問，2026-09-26）

受測 commit：`b8946d2`。受測環境：http://localhost:5173/。唯讀，未改程式。

## 結論：不通過，2 條必修

1. **新炫收藏不會出現在系列頁**：表單發布 `/share/1001`（山線電台／夜行採集／CD／不確定），`/artist/mountain-radio/1` 內 `a[href="/share/1001"]` 計數 0，首頁為 2。原因：`lib/data.ts` 的 `sharesOfSeries` 只讀靜態 `shares`，系列頁 `ShareWall` 沒疊 `state.myShares`。直接違反核心目的「每則炫收藏回流成資料庫」
2. **私訊頁可繞過交易凍結**：`components/inbox.tsx` 的 `sendOffer`／`respondOffer`／`withdrawOffer` 沒有鎖定檢查。對已鎖定的 `/share/6` 在私訊頁可新出價、我要買、賣家接受，localStorage 真的寫入。只有成交被單則頁擋住

## 通過項目

tsc 0 錯；lint 0 錯 1 既有警告；12 頁 × 1440／390 共 24 組 scrollWidth＝innerWidth、console error 0。
動線：清追蹤→熱門藝人→追蹤→首頁變追蹤中 ✅；表單全點選流程 ✅（串接系列頁 ❌）；阿凱未認證檢舉被擋 ✅；小孟檢舉 ✅；/share/8 鎖定→申訴→/admin 解鎖→解除 ✅；爭議版本底下 3 則全鎖 ✅；已知仿冒提醒首頁與單則頁 ✅；舊網址 404 ✅。

## PM 已知問題 13 條

第 5 條（私訊未同步凍結）升級為必修，其餘 12 條可接受。
