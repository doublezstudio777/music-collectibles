# 共用規則（Codex 與 Claude 都適用）

## 這個專案是什麼

台灣樂團、獨立歌手的專輯與周邊，給收藏者用的 C2C 交易平台。
核心不是「又一個拍賣網站」，而是**版本資料庫＋收藏展示**——同一張專輯不同年份、不同贊助、不同授權單位會長得不一樣，這些差異現在沒有任何地方收錄，收藏者的東西也沒有地方被看懂。

自研專案，不屬於任何客戶，沒有交期壓力。

## 跨裝置路徑對照

| 裝置 | 路徑 |
|---|---|
| Mac | `~/AboutAI/專案/music-collectibles/` |
| Windows（主力） | clone 後自訂，建議 `~/AboutAI/專案/music-collectibles/` |
| 手機 | GitHub app／網頁，唯讀為主 |

同步靠 git，不是 OneDrive。**這個 repo 絕對不要搬進 OneDrive**，兩套同步機制會打架、`.git` 會壞。
影音、設計稿等大檔才放 OneDrive，repo 裡只放路徑指向。

## 交棒規則（最重要）

`00_現況.md` 是唯一真相。開工前先讀它，收工前更新它。

- 動手前：讀 `00_現況.md`，確認「當前棒次」是不是自己
- 收工前：更新進度、改「最後更新」那行、把棒子交給下一位
- 一次只有一邊在寫，避免 merge 衝突

### git 流程

**開工 `git pull`，收工 `git add -A && git commit && git push`。**

沒 push 等於沒做——另一台看不到。開工沒 pull 就動手，等著解衝突。

主力在 Windows，Mac 是第二台。兩邊都會動同一份檔案，所以：

- 動手前先 `git pull`
- 一個工作段落就 commit 一次，不要憋一大包
- 收工一定 push，即使做到一半

### 🚨 方向改變時，先更新現況檔再開工

**2026-09-22 的實際教訓**：同一天 Windows 做了「作品優先」規劃、Mac 做了
「分享優先」規劃，方向完全相反。Windows 那份的入口設計直接作廢。

不是誰做錯了。是使用者在其中一邊的對話裡轉了方向，另一邊不知道。

**規則**：使用者改變方向、否決先前決定、或給出新的優先指示時——

1. **第一個動作是更新 `00_現況.md` 並 commit push**，不是先做新規劃
2. 那筆 commit 訊息要明寫「方向改變：舊的 → 新的」
3. 另一邊 `git pull` 後看到這種 commit，先讀完現況檔再動手

**成本對比**：更新現況檔 2 分鐘。兩邊做出互相矛盾的規劃，要花數小時釐清、
作廢、重寫，而且期間所有判斷都建立在錯的前提上。

方向是共用狀態，不是你跟使用者之間的私事。

### node_modules 不同步，這是正常的

`網站/node_modules/`（925 MB）被 gitignore，**不會跟著 git 走**。
`.next/`、`.vinext/`、`.sites-runtime/` 同理，都是本機產物，clone 後自動重建。

### 🚨 裝套件一律用 `npm ci`，不要用 `npm install`

```
cd 網站 && npm ci
```

**原因**：`npm install` 會改寫 `package-lock.json`。macOS 和 WSL/Linux 對
optional 套件的 `libc` 欄位處理不同（Mac 保留 46 筆，Linux 會 prune），
**即使兩台 npm 版本一模一樣也會差**。兩邊各自 install 再 commit，
lockfile 就會在兩台之間無限來回抖動，每次 pull 都是假衝突。

`npm ci` 嚴格按照 lockfile 安裝，**不碰 lockfile**，所以不會有這個問題。

**唯一該用 `npm install` 的時機**：真的要新增或升級套件。那時候：

1. 只在一台做（建議 Windows，主力機）
2. 確認 `git diff 網站/package-lock.json` 的改動是你要的套件異動，不是 `libc` 雜訊
3. commit 並在訊息裡寫清楚裝了什麼
4. 另一台 `git pull` 後跑 `npm ci` 跟上

如果不小心用了 `npm install` 而 lockfile 只有 `libc` 類的雜訊改動：
`git checkout -- 網站/package-lock.json` 還原掉，不要 commit。

## 資料夾規則

| 資料夾 | 放什麼 |
|---|---|
| `討論/` | 跟 AI 的對話存檔。想法、來回辯證、被否決的方案都留著 |
| `研究/` | 競品、市場、法規查證。**每則都要附來源連結** |
| `產出/` | 企劃書、規格、簡報等交付物 |

檔名格式：`YYYYMMDD_主題.md`，對話存檔加來源 `YYYYMMDD_Codex_主題.md`。

舊版不要覆蓋，改名留著或靠 git 歷史。

## 語言規則

一律**台灣繁體中文**。

- 影片不是視頻、資訊不是信息、預設不是默認、品質不是質量、透過不是通過、專案不是項目
- 禁行銷黑話：賦能、閉環、拉新、復購、種草、引流
- 禁中國網路語：牛逼、內卷、家人們、絕絕子
- 自檢：這個詞 PTT／Dcard 會自然講嗎？不會就換

## 工作原則

- **法律問題不要自己下定論**。查到什麼寫什麼、附來源，標「需律師確認」。正式營運前一定要台灣律師看過
- **數字不要腦補**。市場規模、成本估算、用戶數，沒查到就寫「待查證」，不要編一個看起來合理的
- **被否決的想法留紀錄**。寫清楚為什麼否決，避免三個月後又繞回來討論一樣的東西
